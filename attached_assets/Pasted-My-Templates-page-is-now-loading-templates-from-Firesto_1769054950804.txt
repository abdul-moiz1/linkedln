My Templates page is now loading templates from Firestore collection: templates.

Right now it shows:

✅ “No templates found. Please check your Firestore collection.”

Because Firestore collection is empty.

I want you to seed templates automatically into Firestore like Supergrow style.

✅ Goal

Create a Firestore seed system that:

✅ Adds 10–15 LinkedIn carousel templates into Firestore
✅ Templates must be professional LinkedIn style
✅ No random pictures (no dogs, buildings, travel, etc.)
✅ Thumbnails should be clean and business style:

gradients

solid colors

simple shapes

bold typography

✅ Each template must have:

name

type: "carousel"

slidesCount

layout

fields

defaults

status: "active"

isPublic: true

createdAt

updatedAt

✅ Firestore Collection

Seed into:

templates/{templateId}

✅ Template Data To Seed

Seed these templates:

1) basic_001

name: "Basic #1"
slidesCount: 1
layout: "basic_cover"

2) basic_002

name: "Basic #2"
slidesCount: 5
layout: "basic_modern"

3) hook_001

name: "Hook Carousel"
slidesCount: 5
layout: "hook_bold"

4) howto_001

name: "How-To Steps"
slidesCount: 6
layout: "howto_steps"

5) tips_001

name: "5 Quick Tips"
slidesCount: 5
layout: "tips_cards"

6) story_001

name: "Story Format"
slidesCount: 6
layout: "story_minimal"

7) stats_001

name: "Stats & Proof"
slidesCount: 5
layout: "stats_clean"

8) mistakes_001

name: "Mistakes to Avoid"
slidesCount: 6
layout: "mistakes_warning"

9) framework_001

name: "Framework"
slidesCount: 5
layout: "framework_grid"

10) case_001

name: "Mini Case Study"
slidesCount: 6
layout: "case_study"

✅ Common Fields (same for all templates)

Use this array:

["title", "description", "authorName", "authorHandle"]

✅ Defaults (example)

Each template should include defaults like:

defaults: {
  title: "The Power of Data-Driven Marketing",
  description: "A simple guide you can apply today.",
  authorName: "Your Name",
  authorHandle: "@your-handle"
}

✅ Thumbnail Rules (Important)

I do NOT want thumbnails that use random photos.

Instead generate SVG thumbnails automatically for each template:

✅ Make a function generateThumbnailSvg(templateName, layout)
✅ Return a simple branded SVG design
✅ Use big bold title text
✅ Use gradients / clean UI shapes
✅ Export it as a data:image/svg+xml string and store in Firestore thumbnail

Example thumbnail format:

thumbnail: "data:image/svg+xml;utf8,<svg ...>...</svg>"


No external images should be used.

✅ Where to implement seeding

Create a file:

📌 src/scripts/seedTemplates.js

And export:

✅ seedCarouselTemplates() function that:

checks if Firestore already has templates

if none exist → inserts them

if templates already exist → does not duplicate

Use Firestore:

collection()

getDocs()

doc()

setDoc()

serverTimestamp()

✅ Add a Temporary “Seed Templates” Button

On /templates page add a button only visible in development:

✅ Button text: “Seed Templates”
✅ On click:

calls seedCarouselTemplates()

reloads template list

Also auto-run seed once if collection is empty (optional).

✅ Output Needed

Give me working code changes for:

✅ src/scripts/seedTemplates.js
✅ Updated Templates page to include “Seed Templates” button
✅ Ensure templates appear after seeding
✅ Ensure thumbnails are clean LinkedIn style SVG (not random photos)

Implement everything now and make sure Firestore seeding works.